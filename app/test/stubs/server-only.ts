/**
 * Stub for the `server-only` package. Next.js aliases this module
 * internally, so it is not a real dependency; tests alias it here
 * (see vitest.config.js) so `app/i18n/server.ts` can be imported.
 */
export {}
